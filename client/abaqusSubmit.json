
{

    // array containing the abaqus job names for the 
    "jobFiles" : ["C:\\Users\\b.arellano\\Documents\\GitHub\\WAM\\client\\c1.inp", "C:\\Users\\b.arellano\\Documents\\GitHub\\WAM\\client\\c2.inp"],   

    "about" : "",       //optional, description about cases that we are running (recommended size: up to 30 characters)

    "solverFlags" : { 

        /* 

        Any Abaqus solver flags (with the exception of "job=") should be set in this block.You are not limited to the flags shown below. You should be able to specify all of the abaqus flags (ex: number of cpus, gpus, global model, etc.) 

         */

        // number of CPUs used to run the simulations
        "cpus" : 1, 

        // number of GPUs used to run the simulations
        "gpus" : 0,  

        "interactive": null // interactive mode. This flag takes no arguments, so we set it to null  

        // optional, full path to the directory where we are writing the scratch file
        //"scratch" : "/tmp/",  

        // optional, filename for the global model (without the *.inp)
        //"globalmodel" : "myGlobalModel", 

        //"memory" : "12 gb"    // optional, to limit the ammount of memory used to solve the problem

     },

    // abaqus solver, set to "default" to use the system default, or specify full path:
    "solver" : "default", 

    "advanced" : {
        // optional, uncomment below if you wish to receive an email message after each job has been completed
        // "sendEmailTo" :  "None"
    }, 

    "InternalUse" : { // internal use variables, keep as is...
        "jsonFileType": "abaqus",       // internal use - don't touch
        "jsonFileVersion" : "abq0.1"    // internal use - don't touch
    }

}